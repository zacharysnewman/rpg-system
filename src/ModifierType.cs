enum ModifierType
{
    FlatAdditive,        // Added to BaseValue before multipliers are applied
    PercentageAdditive,  // Summed with other percentage modifiers, then applied as a single multiplier
}
